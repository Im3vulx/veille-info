{% extends 'base.html.twig' %}

{% block title %}Neurogameron - Categories
{% endblock %}

{% block body %}
	<section class="categories-page">
		<div class="container">
			<h1 class="categories-title">Catégories</h1>
			<p class="categories-description">
				Explorez nos différentes catégories pour découvrir des articles spécialisés dans chaque domaine. Des langages de programmation à l'intelligence artificielle, de l'espace aux jeux vidéo, vous trouverez ici toutes les informations pour rester à jour.
			</p>

			<div class="category-list">
				{% for category in categories %}
					<a href="{{ path('app_category_show', {id: category.id}) }}" class="category-card">
						<div class="icon">
							<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
								<rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect>
								<line x1="8" y1="21" x2="16" y2="21"></line>
							</svg>
						</div>
						<p>{{ category.name }}</p>
					</a>
				{% endfor %}
			</div>
		</div>
	</section>

	{% include 'footer.html.twig' %}
{% endblock %}
